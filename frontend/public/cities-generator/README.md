# Cities Generator JSON Files

Place your cities JSON files here. The files should be named `{country}.json` (e.g., `it.json`, `uk.json`).

Example structure:
```
public/
  cities-generator/
    it.json
    uk.json
```

The component will automatically load files from this location when using the default `country` prop.

